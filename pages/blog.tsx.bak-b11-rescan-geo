import Head from 'next/head'

import { PRODUCT } from '../lib/product'
import { buildFaqJsonLd, buildHowToJsonLd } from '../lib/schema'

const SITE = 'https://jsonschema.lxsaihub.com'
const PUBLISHED = '2026-09-17'

const faqs = [
  {
    "question": "What is JSONSchema?",
    "answer": "JSONSchema is a tool that turns a sample JSON object into a strict JSON Schema with explanations and validation rules."
  },
  {
    "question": "What schema version does it use?",
    "answer": "It produces JSON Schema draft 2020-12, the current standard most validators support."
  },
  {
    "question": "Does it mark required fields?",
    "answer": "Yes. It marks required fields automatically from the sample."
  },
  {
    "question": "Does it explain the rules?",
    "answer": "It explains each validation rule in plain language so you know what the API should enforce."
  },
  {
    "question": "Who should use it?",
    "answer": "API developers who want a strict, documented contract from a sample object."
  },
  {
    "question": "Is it compliant with modern tooling?",
    "answer": "Draft 2020-12 compliance means the schema works with current validators."
  }
] as { question: string; answer: string }[]

const howToBlocks = [
  {
    "name": "How to run a first pass with JSONSchema",
    "steps": [
      {
        "name": "Open the tool",
        "text": "Go to https://jsonschema.lxsaihub.com and open the main workspace for JSONSchema."
      },
      {
        "name": "Enter your inputs",
        "text": "Fill the fields the product asks for. Start with the shortest complete input that still reflects your real case."
      },
      {
        "name": "Generate the draft",
        "text": "Run JSONSchema to get a structured draft you can read in plain English."
      },
      {
        "name": "Review before you act",
        "text": "Edit anything that does not match your context. Treat the output as decision support, not a final authority."
      }
    ]
  }
] as { name: string; steps: { name: string; text: string }[] }[]

type Post = { slug: string; title: string; type: string; query: string; body: string }
const posts: Post[] = [
  {
    slug: 'what-is-jsonschema',
    title: 'What is JSONSchema?',
    type: 'Definitional · FAQPage',
    query: 'what is jsonschema',
    body: 'JSONSchema is a tool that turns a sample JSON object into a strict JSON Schema with explanations and validation rules.'
  },
  {
    slug: 'what-is-jsonschema-2',
    title: 'What is JSONSchema?',
    type: 'Definitional · FAQPage',
    query: 'what is JSONSchema',
    body: 'JSONSchema turns a sample JSON object into a strict JSON Schema (draft 2020-12), explains each required field, and lists the validation rules your API should enforce.'
  },
  {
    slug: 'what-schema-version-does-it-use',
    title: 'What schema version does it use?',
    type: 'How-to',
    query: 'what schema version does it use',
    body: 'It produces JSON Schema draft 2020-12, the current standard most validators support.'
  },
  {
    slug: 'does-it-mark-required-fields',
    title: 'Does it mark required fields?',
    type: 'How-to',
    query: 'does it mark required fields',
    body: 'Yes. It marks required fields automatically from the sample.'
  },
  {
    slug: 'does-it-explain-the-rules',
    title: 'Does it explain the rules?',
    type: 'How-to',
    query: 'does it explain the rules',
    body: 'It explains each validation rule in plain language so you know what the API should enforce.'
  },
  {
    slug: 'jsonschema-vs-manual',
    title: 'JSONSchema vs doing it manually',
    type: 'Comparison',
    query: 'jsonschema vs manual',
    body: 'Use JSONSchema when you need a fast, structured first draft aligned to its stated job. Manual work still wins for one-off edge cases or when you already have a polished template. The product removes blank-page cost; you keep final judgment.'
  }
]

export default function Page() {
  const NAME = PRODUCT.name
  return (
    <>
      <Head>
        <link rel="canonical" href={`${SITE}/blog`} />
        <meta property="og:url" content={`${SITE}/blog`} />
        <meta property="og:type" content="website" />

        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(buildFaqJsonLd(faqs)) }}
        />
        {howToBlocks.map((block, i) => (
          <script
            key={`howto-${i}`}
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify(buildHowToJsonLd(block.name, block.steps)),
            }}
          />
        ))}
        {posts.map((p, i) => (
          <script
            key={`article-${i}`}
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify({
                '@context': 'https://schema.org',
                '@type': 'Article',
                '@id': `${SITE}/blog`,
                mainEntityOfPage: { '@type': 'WebPage', '@id': `${SITE}/blog` },
                headline: p.title,
                description: p.body,
                inLanguage: 'en',
                datePublished: PUBLISHED,
                dateModified: PUBLISHED,
                author: { '@type': 'Organization', name: NAME, url: SITE },
                publisher: {
                  '@type': 'Organization',
                  name: NAME,
                  url: SITE,
                  logo: { '@type': 'ImageObject', url: `${SITE}/logo.png` },
                },
              }),
            }}
          />
        ))}

        <title>{`${NAME} — Blog`}</title>
        <meta name="description" content="JSONSchema turns a sample JSON object into a strict JSON Schema (draft 2020-12), explains each required field, and lists the validation rules your API should en" />
      </Head>
      <div className="min-h-screen bg-slate-50 text-slate-800">
        <header className="border-b border-slate-200 bg-white">
          <div className="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" className="font-bold text-slate-900">{NAME}</a>
            <nav className="hidden md:flex gap-6 text-sm font-semibold text-slate-500">
              <a href="/use-cases" className="hover:text-slate-900">Use cases</a>
              <a href="/integrations" className="hover:text-slate-900">Integrations</a>
              <a href="/how-it-works" className="hover:text-slate-900">How it works</a>
              <a href="/security" className="hover:text-slate-900">Security</a>
              <a href="/blog" className="hover:text-slate-900">Blog</a>
            </nav>
          </div>
        </header>
        <main className="max-w-3xl mx-auto px-6 py-14">
          <h1 className="text-3xl font-bold text-slate-900">Blog</h1>
          <p className="mt-3 text-slate-600">Definitional and how-to posts that help search engines and humans understand {NAME}.</p>

          <section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">What it does</div>
              <p className="mt-2 text-sm text-slate-700">{PRODUCT.description || 'JSONSchema turns a sample JSON object into a strict JSON Schema (draft 2020-12), explains each required field, and lists the validation rules your API should enforce.'}</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Who it&apos;s for</div>
              <p className="mt-2 text-sm text-slate-700">People who need JSONSchema's specific outcome without building the workflow from scratch.</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Input → output</div>
              <p className="mt-2 text-sm text-slate-700">Enter the product inputs → review a structured plain-English draft → edit before you act.</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Note</div>
              <p className="mt-2 text-sm text-slate-700">Decision-support drafts only. English final review remains a human gate.</p>
            </div>
          </section>

          <div className="mt-10 grid gap-5 sm:grid-cols-2">
          {posts.map((p) => (
            <article key={p.slug} className="rounded-xl border border-slate-200 bg-white p-6">
              <div className="text-xs font-semibold text-indigo-600 mb-1">{p.type}</div>
              <h3 className="font-semibold text-slate-900">{p.title}</h3>
              <p className="mt-2 text-sm text-slate-600">{p.body}</p>
            </article>
          ))}
          </div>
        </main>
        <footer className="border-t border-slate-200 bg-white">
          <div className="max-w-5xl mx-auto px-6 py-8 text-sm text-slate-500 flex flex-wrap gap-6">
            <a href="/security" className="hover:text-slate-900">Security</a>
            <a href="/use-cases" className="hover:text-slate-900">Use cases</a>
            <a href="/integrations" className="hover:text-slate-900">Integrations</a>
            <a href="/how-it-works" className="hover:text-slate-900">How it works</a>
            <a href="/blog" className="hover:text-slate-900">Blog</a>
          </div>
        </footer>
      </div>
    </>
  )
}
