# JSONSchema

> Turn sample JSON into a strict, valid JSON Schema

JSONSchema jSONSchema turns a sample JSON object into a JSON Schema (draft 2020-12), explains each required field, and screens it with a deterministic compliance ruleset (type/required/format/enum). Export the schema your API can enforce.

**Get started:** [Try JSONSchema](/) — runs in your browser, no setup.
